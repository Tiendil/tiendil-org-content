---
title = "DepMesh — делаем зависимости между файлами частью агентской архитектуры"
tags = ["practice", "development", "open-source", "neural-networks", "prompt-engineering", "large-language-models", "exocortex", "ai-agents", "coding-agents"]
published_at = "2026-06-06T12:00:00+00:00"
seo_description = "DepMesh предоставляет вашим агентам универсальный интерфейс к извлекчению зависимостей между файлов."
seo_image = "cover.jpg"
---

Представим, вашему агенту надо изменить файл с кодом, например, реализовать новую фичу.

Естественно, для этого ему надо знать что использует этот исходник, что используется исходником, где лежат тесты, какие спеки содержат требования к этой части проекта и так далее.

По-умолчанию агенты делают это влоб:

- читают оригинальный файл;
- анализируют его импорты и прочий контент (тратят токены);
- генерируют пути к файлам с импортируемыми символами (тратят токены) и читают их;
- грепают или как-то ещё ищут зависимые файлы (тратят токены), которые используют код из целевого исходниука;
- то же самое делают со спеками (тратят токены).

Между этими шагами, конечно, агенты думают как делать следующий шаг (тратят токены).

Вся эта тягомотина ест контекст, занимает время и, несмотря на затраченные усилия, не гарантирует надёжное наполнение контекста необходимой информацией. Утрируя, агент может забыть погрепать по имени какой-нибудь функции и потерять важную зависимость, или пропустить нишевую спеку, потому что решил, что вот в данном случае она не нужна.

Более того, для поиска агент задействует кучу тулов, каждая из которых имеет собственные нюансы, такие как уровень точности результата.

Общепринятые способы улучшить ситуацию, это интеграция [LSP](https://microsoft.github.io/language-server-protocol/) или чего-то подобного в качестве одного из инструментов агента или разворачивание одной из миллиона [RAG-систем](https://ru.wikipedia.org/wiki/Генерация_с_дополненной_выборкой). Это помогает делу, но не снимает все проблемы. Например, не гарантирует полноту, минимальность и детерминированность результата.

В то же время, с древних времён у нас есть огромный пул утилит и библиотек для анализа исходного кода, которые умеют быстро и детерминированно искать и анализировать файлы, без всяких там LLM. Каждая из них в своей конкретной области работает намного лучше [вероятностной модели]{post:ai-notes-2024-generative-knowledge-base}.

Было бы здорово, подумал я, иметь одну абстракцию над всеми этими инструментами, которая предоставляет агенту универсальный интерфейс для извлечения любых зависимостей между файлами (какие настроите) и не требует от него думать что и каким образом искать.

Например, вот так:

```bash
> depmesh -p llm dependencies -r governed_by -r tested_by ./depmesh/cli/application.py

## governed_by

Specifications that apply to the artifact.

- @/specs/architecture/entities.md
- @/specs/architecture/errors.md
- @/specs/architecture/modules_layout.md
- @/specs/architecture/naming.md
- @/specs/architecture/static_analysis.md
- @/specs/architecture/tests.md
- @/specs/behavior/cli.md
- @/specs/behavior/file_paths.md

## tested_by

Tests that verify the artifact.

- @/depmesh/cli/tests/test_application.py
```

Так и появился [DepMesh](https://github.com/Tiendil/depmesh).

<!-- more -->

## Как работает DepMesh

DepMesh — это CLI утилита с конфигурационным файлом `depmesh.toml` в корне вашего проекта.

В конфиге вы указываете две вещи:

1. Типы зависимостей между файлами.
2. Правила поиска этих зависимостей для файлов.

Простейший конфиг может выглядеть так:

```toml
version = 1

[[relations]]
id = "tested_by"
description = "Tests that verify the artifact."

[[rules]]
relation = "tested_by"
input = { type = "glob", pattern = "./src/{**package_path}/{*module}.py" }
output = { type = "files", pattern = "./src/{package_path}/tests/test_{module}.py" }
```

В нём мы определяем один тип зависимости `tested_by` и правило «Каждый исходный файл `./module.py`  связан отноешнием `tested_by` с файлом `./tests/test_module.py`».

Кроме glob-паттернов из примера выше, DepMesh поддерживает вызов сторонних утилит, поэтому вы можете анализировать файлы как душе угодно: использовать линтеры, LSP-серверы, парсить исходники регулярными выражениями, ходить в базу данных, вызывать HTTP API и так далее.

Примеры реальных конфигов можно найти в репозитории самой утилиты  [depmesh/depmesh.toml](https://github.com/Tiendil/depmesh/blob/main/depmesh.toml), или, для примера, в репозитории моей читалки новостей [feeds.fun/depmesh.toml](https://github.com/Tiendil/feeds.fun/blob/main/depmesh.toml).

Подробную документацию по работе с утилитой и её настройке можно найти в [depmesh/README.md](https://github.com/Tiendil/depmesh/tree/main).

## Как я использую DepMesh
